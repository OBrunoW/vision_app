name: vision_app
description: RTMP camera streaming app
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter
  camera: ^0.10.5+9
  permission_handler: ^11.3.0
  shared_preferences: ^2.2.2
  wakelock_plus: ^1.1.4
  rtmp_streaming: ^1.0.5

dev_dependencies:
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
